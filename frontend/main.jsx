import React from "react";
import ReactDOM from "react-dom/client";
import JarvisDigitalTwin from "./JarvisDigitalTwin";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JarvisDigitalTwin />
  </React.StrictMode>
);
